# =============================================================================
# TrendsMix — Environment Variables
# =============================================================================
#
# SETUP:
#   1. Copy this file:  cp .env.example .env.local
#   2. Fill in the values below
#   3. Restart the dev server:  npm run dev
#
# =============================================================================

# ---------------------------------------------------------------------------
# Site URL  (optional for local dev, required for production)
# ---------------------------------------------------------------------------
# Used for canonical URLs, sitemap, Open Graph images.
# On Vercel, defaults to the deploy URL if not set.
NEXT_PUBLIC_SITE_URL=https://www.trendsmix.online

# ---------------------------------------------------------------------------
# Supabase  (required)
# ---------------------------------------------------------------------------
# Where to find these values:
#   Supabase Dashboard → Your Project → Settings → API
#
# "Project URL"  →  paste as NEXT_PUBLIC_SUPABASE_URL
# "anon public"  →  paste as NEXT_PUBLIC_SUPABASE_ANON_KEY
# ---------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ---------------------------------------------------------------------------
# n8n automation  (required for auto-publishing via /api/posts)
# ---------------------------------------------------------------------------
# Generate a random secret:  openssl rand -hex 32
# Use the same value in n8n as the x-api-key header.
# ---------------------------------------------------------------------------
N8N_API_KEY=

# Supabase service role key — Dashboard → Settings → API → service_role
# NEVER expose this in the browser. Server-only.
SUPABASE_SERVICE_ROLE_KEY=
